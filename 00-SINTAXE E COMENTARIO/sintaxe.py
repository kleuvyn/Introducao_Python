'''
Sintaxe:
        Python é conhecido por sua sintaxe limpa e fácil de entender.
        Usa indentação para *definir blocos de código*, em vez de chaves ou palavras-chave como "begin" e "end".
        Usa dois pontos (:) para *iniciar blocos de código* (como loops, funções, condicionais, etc.).
        Usa parênteses para *agrupar* expressões.
        Usa aspas simples (' ') ou duplas (" ") para *delimitar strings*.
'''

# Exemplo de função que retorna o dobro de um número
def dobro(numero):
    return numero * 2

resultado = dobro(5)
print(resultado)

'''snake case
    Formato:
        Todas as letras são minúsculas.
        As palavras são separadas por underscores (_).
    Convenções:
        É uma convenção comum usar snake case para nomes de variáveis, funções e atributos.
        Ajuda a tornar o código mais legível, especialmente em variáveis com nomes compostos por múltiplas palavras.
'''
nome_completo = "João Silva"
idade_usuario = 25
saldo_conta = 1000.50
